version: 3
dependencies:
  services:
    - service:
        description: Orders API
        definitions:
          - definition:
              source:
                filesystem:
                  directory: .
              specs:
                - spec:
                    id: ordersApi
                    path: ./contracts/orders_api.yaml

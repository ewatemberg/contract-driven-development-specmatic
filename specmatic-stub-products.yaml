version: 3
dependencies:
  services:
    - service:
        description: Products API
        definitions:
          - definition:
              source:
                filesystem:
                  directory: .
              specs:
                - spec:
                    id: productsApi
                    path: ./contracts/products_api.yaml
